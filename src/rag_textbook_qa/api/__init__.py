"""Public question-answering API, chat page, and the guards in front of them.

Import `rag_textbook_qa.api.app` only when serving, because it needs the `api` extra.
`rag_textbook_qa.api.guard` has no third-party dependency.
"""
